# Engineering Log

Running, dated history of what was built and what was learned. Newest entry on top. One entry per meaningful unit of work — especially anything that broke.

**Entry format:**
```
## YYYY-MM-DD — <short title>
**Agent**: <which subagent, or "pm"/"kartikey">
**Did**: <what was built/changed>
**Issue**: <what broke, if anything — omit if nothing broke>
**Root cause**: <why it broke>
**Fix**: <what resolved it>
**Lesson**: <what to do differently next time — this is the part worth keeping>
```

---

## 2026-08-01 — Week 5: service alerts, scorecard, docs, and a real overflow fix
**Agent**: data-engineer-agent, backend-engineer-agent, frontend-engineer-agent, docs-writer-agent
**Did**: with real GTFS-RT flowing, filled in the endpoints the architecture always planned but never got built. Found NJT's real alerts endpoint (`getStationMSG`) the same way as the vehicle-data endpoint — narrow fetches against the reference client's real source rather than its README — and tested it live: 7 real active system-wide alerts, 6 resolving to Newark-area lines via a new `match_line_scope()` helper (the alerts feed's `MSG_LINE_SCOPE` has its own quirks: a leading `*` and inconsistent casing like `"MontClair-Boonton Line"`, so this is a case-insensitive match, not a dict lookup). Built `poll_alerts.py`, ran it for real, confirmed real alert rows landed (including a live Princeton Dinky maintenance advisory). Added `GET /alerts` and `GET /lines/{line}/scorecard` (rolling 7/30-day on-time %, deduplicated to one reading per trip per calendar day so a trip re-polled every 5 minutes isn't overcounted) to the backend, tested both against real data. Wired both into the dashboard — verified in-browser with a real screenshot showing a real 50%/6-trip scorecard and the real Princeton Dinky alert. Added the alerts poll to the `ingest.yml` schedule. Rewrote the root `README.md` (was still stuck at "Day 0") with a real mermaid architecture diagram and wrote `docs/demo-script.md` for actual conference/meetup use.
**Issue**: found the live status table (`/dashboard`) had no horizontal-scroll wrapper — a 5-column table would overflow on narrow/mobile viewports rather than scrolling within its own container.
**Root cause**: added when the table was first built (Week 1) without a mobile pass; never caught since verification up to that point was all desktop-width.
**Fix**: wrapped the table in `overflow-x-auto`. Reviewed the rest of the ingestion layer's error handling while at it (timeouts, DB rollback-on-exception, explicit auth-failure errors) and found it was already solid — no further changes needed there.
**Lesson**: same pattern as the GTFS-RT and alerts endpoints — when the third-party reference client's docs/README under-specify something, go get the real behavior directly (narrow, specific fetches, not broad ones that trigger copyright-reproduction refusals) rather than layering guess on guess. Also: a desktop-only verification habit will systematically miss mobile-only bugs; worth a deliberate narrow-viewport pass, not just relying on whatever the default screenshot happens to show.

---

## 2026-08-01 — NJT RailData access approved: real GTFS-RT ingestion is live
**Agent**: data-engineer-agent
**Did**: Kartikey's RailData API registration was approved. Rather than guess at endpoint shapes, pulled the actual working request/response structure from a public reference client's real source code (github.com/jtarrio/raildata/api/{api,methods}.go, not just its README) via targeted, narrow fetches: confirmed base URL (`https://raildata.njtransit.com/api/TrainData`), that every call is POST with **multipart/form-data** (not JSON, not urlencoded — `requests`' `files={k: (None, v)}` trick reproduces this), and the exact field names (`Username`/`Password` → `Authenticated`/`UserToken`; `getVehicleData` returns a **bare JSON array**, no wrapper object). Then tested every step against the real live API with Kartikey's actual credentials: token fetch succeeded, `getVehicleData` returned 30 real currently-running trains system-wide. Rewrote `njt_client.py` and `poll_gtfs_rt.py` to match, ran `poll_gtfs_rt.py` for real, and confirmed 23 real trip_updates rows landed in production Postgres across all 6 Newark-area lines (verified by querying row counts and sample rows, including one train whose real `NEXT_STOP` is literally "Newark Broad"). Set `NJT_USERNAME`/`NJT_PASSWORD` as GitHub Actions secrets and removed the `continue-on-error` workaround on the ingest workflow's NJT step, since failure is no longer expected.
**Issue 1**: the live feed's `TRAIN_LINE` field uses full descriptive names ("Northeast Corridor Line", "Main Line", "Bergen County Line") that don't match static GTFS's `route_short_name` codes, and don't even always match its `route_long_name` ("Northeast Corridor Line" vs. static GTFS's "Northeast Corridor" — the live feed adds "Line"). Also discovered "Main Line" and "Bergen County Line" are reported as two separate live-feed names despite static GTFS combining them into one route (`MNBN`, "Main/Bergen County Line") — real-world naming doesn't cleanly map to either of our prior assumptions.
**Root cause**: static GTFS and the real-time vehicle feed are two independently-maintained NJT systems with no shared naming convention.
**Fix**: queried the live feed for every distinct `TRAIN_LINE` value across all currently-running trains system-wide (31 trains, 10 lines) to get a complete, verified list rather than guessing from a handful of samples, and built `TRAIN_LINE_TO_CODE` in `config.py` from that.
**Issue 2**: `zoneinfo.ZoneInfo("America/New_York")` failed locally with `ZoneInfoNotFoundError` — Windows doesn't ship IANA tzdata the way Linux does.
**Fix**: added the `tzdata` package to `ingestion/requirements.txt` (harmless no-op on Linux/GitHub Actions, required on Windows).
**Issue 3**: `NEXT_STOP` (live feed) is a station *name* ("Ridgewood"), while our `stops` table's `stop_id` is a numeric static-GTFS ID ("107") — no shared key exists between the two systems.
**Fix**: store the station name as-is in `trip_updates.stop_id` rather than building a fuzzy name-matching layer now; documented as a known limitation (that column isn't currently joinable against `stops.stop_id`) rather than silently pretending it is.
**Lesson**: when a third-party reference client's README summary isn't enough (it wasn't, initially — it didn't specify multipart encoding or the bare-array response), go get the actual source code with narrow, specific fetch requests rather than broader ones that trigger copyright-reproduction refusals. And once real credentials exist, *use them* to empirically verify field values (the live `TRAIN_LINE` naming) rather than continuing to reason from documentation/reference-client structure alone — the reference client confirmed the shape correctly but couldn't have told us "Main Line" and "Bergen County Line" are reported separately; only a real live query surfaced that.

---

## 2026-08-01 — Landing page v4: dark/mysterious, monumental hero, technical marginalia
**Agent**: frontend-engineer-agent (self-instructed)
**Did**: Kartikey pointed to zajno.com plus a specific methodology example (a "Kestrel" landing-page prompt showing a monumental processed image, technical marginalia, extreme type contrast, near-monochrome + single accent, and an explicit list of clichés to avoid) and asked for "dark mysterious, calm dynamic image in the background, very Pinteresty." Rebuilt the landing page a fourth time: dropped v3's lavender/rose color-block sections and hand-illustrated train entirely in favor of one `--ink` background throughout, one `--signal` amber accent (evoking platform lighting), a crafted CSS/SVG `HeroScene` (dark vignette + one warm glow + faint converging rail-perspective lines) standing in for a real monumental photo, technical marginalia (real Newark Penn Station coordinates, plate number, timestamp), a flat minimal top nav replacing the floating pill, a mono "ledger" list replacing the colored pipeline cards, and an outlined/ghost wordmark divider. Per-line categorical colors moved to being dashboard-only, since showing 6 bright colors on the landing page would violate this direction's own "near-monochrome" rule. Verified lint, build, and a full in-browser scroll-through.
**Decision**: asked Kartikey whether he had a real photo to use as the actual hero image (most authentic option) vs. building a crafted placeholder now; he chose the placeholder. `HeroScene.tsx` is sized/positioned exactly where a real image goes later, per the reference methodology's own explicit instruction to reserve the hero slot without generating or sourcing imagery prematurely.
**Lesson**: this is the third design correction (v1→v2→v3→v4). Compounding pattern: v1's mistake was being restrained in the wrong dimension (motion/imagery), not palette — v4 is *also* near-monochrome, but it works this time because the restraint is deliberate and paired with extreme typography and one meaningful accent, not an absence of care. "Near-monochrome" and "boring" are not the same failure mode; the difference is whether every remaining element (marginalia, type scale, the one accent, the one image) is doing real work.

---

## 2026-07-31 — Security audit: repo is clean, one info-disclosure redacted
**Agent**: pm-agent
**Did**: full audit of the pushed repo per Kartikey's request — entire git history scanned for the DB password/connection strings (never committed), all `.env` files confirmed never tracked, GitHub Actions run logs checked for secret leakage (none), all tracked files scanned for AWS/GitHub/OpenAI-shaped secret patterns (none), Python/frontend code scanned for injection-prone patterns (`eval`/`exec`/`pickle`/raw SQL/`dangerouslySetInnerHTML`, none found), CORS config reviewed (scoped origins, GET-only, no credentials), workflow YAML reviewed for script-injection via untrusted context (none — secrets only ever flow through `env:`, never string-interpolated into `run:`).
**Issue**: the real Supabase project hostname (`db.<project-ref>.supabase.co`) was written into an earlier `ENGINEERING_LOG.md` entry describing the IPv6 bug, and pushed to the public repo. Not a credential — the project ref alone doesn't grant DB access — but it's unnecessary infrastructure fingerprinting with no upside to keeping public.
**Root cause**: pasted the real error message verbatim into the log entry for accuracy/debuggability, without considering that the hostname it contained would become permanently public.
**Fix**: redacted to `db.<project-ref>.supabase.co` in this file. (Still present in earlier git history — acceptable, since it's not a credential and rewriting public git history has its own costs.)
**Lesson**: when logging a real error message for debugging purposes, check it for embedded infrastructure identifiers (hostnames, project IDs, internal IPs) before pasting it into a doc that's about to be pushed to a public repo, not just for the credential itself.

---

## 2026-07-31 — Supabase connected, real ingestion runs, GitHub Actions IPv6 bug found
**Agent**: pm-agent, devops-engineer-agent
**Did**: Kartikey created a Supabase project. Wired `DATABASE_URL` into `/ingestion`, `/backend`, `/ml`'s local `.env` files; ran `init_db()` and both `poll_weather.py` and `static_gtfs_loader.py` for real against production Supabase (156 weather rows, 17 routes, 231 stops -- verified with a row-count query, not just "no errors"). Started backend + frontend locally against the real database and confirmed the dashboard renders real data end-to-end. Created the GitHub repo (`senseikartikey/ontrack-newark`, public), pushed, set `DATABASE_URL`/`WEATHER_USER_AGENT` as repo secrets, and manually triggered all three Actions workflows to prove they actually run in CI, not just locally.
**Issue**: all three workflow runs failed identically: `connection to server at "db.<project-ref>.supabase.co" (2600:...), port 5432 failed: Network is unreachable`.
**Root cause**: the connection string Kartikey provided was Supabase's *direct* database connection string, which resolves to an IPv6-only address. GitHub Actions runners don't have IPv6 connectivity. It worked locally only because this machine has IPv6 connectivity (or a dual-stack path) that GitHub's runners don't -- so the exact same code and connection string succeeded in one environment and failed in the other for a network-layer reason that had nothing to do with the code.
**Fix**: need the Supabase **pooler** connection string instead (Project Settings → Database → Connection pooling → Transaction mode → URI, port 6543, IPv4-compatible) in place of the direct one, both in local `.env` files and the `DATABASE_URL` GitHub secret.
**Lesson**: "it worked when I ran it locally" is not sufficient verification for infrastructure that's meant to run somewhere else (CI, a different host) -- network path differences (IPv4/IPv6 support, egress rules) are invisible until tested in the actual target environment. The instruction to use the pooler string was given up front specifically to avoid this, but the direct string still got used since it's easy to grab the wrong one from Supabase's dashboard; worth double-checking the actual string's port (5432 = direct, 6543 = pooler) before trusting it rather than assuming the user grabbed the right one.

**Resolution (same day)**: Kartikey provided the actual pooler URI (`aws-0-us-east-1.pooler.supabase.com:6543`). Swapped it into all three services' local `.env` files and the `DATABASE_URL` GitHub secret, re-verified it worked locally, then re-triggered all three workflows and confirmed each one (`Ingest live data`, `Refresh static GTFS`, `Recompute delay baseline`) completed with `conclusion: success` by run ID -- not just eyeballing a list. Re-queried row counts afterward and saw `weather_hourly` go from 156→157, confirming genuinely fresh data landed from the CI run rather than a stale pre-existing count. **OnTrack Newark's infrastructure is now fully live end-to-end on real infrastructure**: public GitHub repo, scheduled Actions workflows, real Supabase Postgres. Only NJ Transit's RailData approval (pending, 2-3 days) stands between this and real rail delay data.

---

## 2026-07-31 — NJ Transit developer registration submitted
**Agent**: pm-agent
**Did**: Kartikey registered at developer.njtransit.com for RailData API access. NJT says 2-3 days for approval.
**Status note**: this unblocks `njt_client.py`/`poll_gtfs_rt.py`'s TODOs (confirming real endpoint paths/response shapes) once approved — but does **not** block everything. The weather poller and static GTFS loader need no NJT credentials at all, only a real Postgres DB (Supabase), which is still the other pending manual step (see `/ingestion/README.md`). Setting up Supabase now, independent of the NJT wait, would let real weather + static GTFS data start accumulating today rather than waiting another 2-3 days for nothing.
**Lesson**: when a task has multiple external blockers, check whether they're actually independent before treating the whole pipeline as blocked on the slowest one.

---

## 2026-07-31 — Week 3: ML baseline, /predict endpoint, dashboard wiring, real infra
**Agent**: ml-engineer-agent, backend-engineer-agent, frontend-engineer-agent, devops-engineer-agent (all self-instructed, per the subagent-discovery limitation noted 2026-07-30)
**Did**:
- `/ml`: v1 statistical baseline (`compute_baseline.py`) -- average delay per (line, hour-of-day, day-of-week) bucket, written to a new `delay_baseline` table, gated by a minimum-sample threshold. Deliberately dropped the weather-bucket dimension from the original plan for v1 (finer buckets = sparser samples, and there's zero real data right now anyway); documented as a fast-follow. Verified the aggregation logic against synthetic seed data: confirmed trip-level dedup (a trip polled multiple times counts once), correct weekday bucketing, and correct threshold filtering, all with hand-computed expected values.
- `/backend`: added `GET /lines/{line}/predict`, reading the current hour/day-of-week's baseline bucket. Returns `{"status": "insufficient_data", ...}` honestly when there's no trustworthy bucket (true for every line today) rather than fabricating a number. Smoke-tested both the empty-data and real-data paths (seeded a fake baseline row to confirm the "has data" response shape), plus the 404 path.
- `/frontend`: wired a "Predicted delay risk" panel into the dashboard, showing a colored risk badge + explanation when data exists, and an honest explanation of *why* there's nothing to show when it doesn't. Verified both states in-browser.
- `/infra`: built the GitHub Actions workflows (`ingest.yml`, `static-gtfs.yml`, `baseline.yml`) that `/ingestion`'s and `/ml`'s READMEs have referenced since Week 1 but that never actually existed until now. Validated all three YAML files parse correctly.
**Issue**: the plan's "60-120s polling" assumption doesn't fit GitHub Actions' cron (no finer than 1-minute granularity, and scheduled runs aren't guaranteed to fire exactly on time). Also: a private GitHub repo only gets 2000 free Actions minutes/month, which a 5-minute ingestion cadence would blow through in days.
**Root cause**: the original plan described the *ideal* polling interval for a dedicated always-on worker, not what's achievable on GitHub Actions' free tier, which was the chosen ingestion mechanism specifically to avoid paying for a worker.
**Fix**: settled on a 5-minute cadence as the practical floor, and documented in `/infra/README.md` that this repo should stay public specifically so Actions minutes are unlimited -- reducing polling frequency to fit a private-repo budget would be the wrong tradeoff for a project whose entire pitch is "portfolio piece."
**Lesson**: when a plan specifies a technical parameter (a polling interval, a threshold) based on an idealized version of the infrastructure, check it against the actual chosen infrastructure's real constraints before building against it -- the free-tier choice (GitHub Actions instead of a paid worker) was made explicitly in the plan, but its knock-on constraint on polling frequency wasn't derived until actually writing the workflow.

---

## 2026-07-31 — Landing page v3: full pivot to illustrated creative-agency register
**Agent**: frontend-engineer-agent (self-instructed, per the subagent-limitation note in the entry below)
**Did**: Kartikey said v2 (colorful dev-tool page from the day before) still wasn't it, and pointed to a specific reference: [Web design for SAS Design Studio, by Władysław for Zajno](https://dribbble.com/shots/24257855-Web-design-for-SAS-Design-Studio) — an illustrated creative-agency style (film grain, bold lavender/dusty-rose color-block sections, flat silhouette illustration, floating pill nav, oversized wordmark section), plus two GitHub-hosted design-taste skills (`pbakaus/impeccable`, `Leonxlnx/taste-skill`) and a request to connect the Higgsfield AI MCP server for illustration generation. Rebuilt the landing page around the reference: new `--ink`/`--lavender`/`--rose` tokens (tinted, never pure black, per `impeccable`'s rules), a `.grain` CSS utility (SVG fractal-noise texture) applied to every section, a hand-built flat-silhouette train/station illustration (`StationIllustration.tsx`) since no image-gen tool is live yet, a floating pill nav (`PillNav.tsx`), an oversized lowercase "ontrack" wordmark divider section, and scroll-linked parallax on the illustration via `motion`. Removed the now-superseded `AuroraBackground`/`RailMapVisual` components rather than leaving them as dead code. Re-verified lint, production build, and an in-browser walkthrough of every section after the rebuild (one section briefly rendered low-contrast mid-scroll during testing — confirmed to be the scroll-reveal `IntersectionObserver` catching up after a fast programmatic scroll, not a real bug, by re-screenshotting after a short wait).
**Issue**: two things I could not do myself. (1) The `impeccable`/`taste-skill` repos install via `npx impeccable install` / `npx skills add` — running an unfamiliar npm package's code autonomously falls under "don't execute files from untrusted sources," so I read their documented principles instead and applied them by hand rather than running the installers. (2) The Higgsfield MCP can't be made live within a running conversation — there's no tool for that — and it almost certainly needs an API key Kartikey hasn't supplied.
**Fix**: added a `.mcp.json` declaring the Higgsfield server so it's ready for Kartikey's own approval/credentials in a future session; documented in `docs/landing-page-brief.md` that the hand-built illustration is a stand-in worth revisiting once/if that connection is live.
**Lesson**: this is the second real design correction in two days (see the 2026-07-30 entry below for the first). Compounding lesson: a specific visual reference (an actual Dribbble shot) is far more useful to work from than a verbal style description ("Dribbble-caliber," "colorful and animated") — the gap between v2 and v3 is much bigger than the gap between v1 and v2 was, even though v1→v2 already added real color and motion. When a redesign request references a specific external example, go look at the actual example (screenshot it, scroll through it) before touching code, rather than working from the verbal description of what "more attractive" might mean.

---

## 2026-07-30 — Landing page redesign: color, motion, and a subagent transparency note
**Agent**: frontend-engineer-agent (see note below), pm-agent
**Did**: Kartikey reviewed the Week 2 landing page and called it boring, flat, and generic-looking despite following the anti-slop brief — no color beyond one blue accent, no animation, plain typography. Rebuilt the hero and every section: an animated color-coded SVG diagram of the real Newark rail network (lines + hub stations, small dots animating along each path via native SVG `<animateMotion>`) replaces the flat terminal box as the primary hero visual; added a slow-drifting multi-hue aurora background and gradient hero text (both built from the `dataviz` skill's categorical palette, not arbitrary colors); added `motion` (Framer Motion) for staggered hero entrance, scroll-reveal on every section, and hover micro-interactions; swapped the display font from Space Grotesk to the bolder Bricolage Grotesque; gave each of the 6 distinct Newark-area lines a fixed identity color (used consistently in the hero map, the "how it works" step accents, the pipeline diagram, and the dashboard's line pills). Re-verified lint, production build, and an in-browser look at both themes after the rebuild.
**Issue**: When asked to build a "sub-team of agents, one per department," I actually tried invoking `subagent_type: "frontend-engineer-agent"` for the first time this session (prompted by Kartikey directly asking "where is the frontend engineer agent") and it failed: `Agent type 'frontend-engineer-agent' not found. Available agents: claude, claude-code-guide, Explore, general-purpose, Plan, statusline-setup`.
**Root cause**: the `.claude/agents/*.md` files created on Day 0 are real, valid Claude Code subagent definitions, but this session's `Agent` tool only recognizes a fixed built-in list — it never picked up the project's custom agents. Every "agent" credited in this log for Week 1/Week 2 work was actually me, single-threaded, applying that persona's brief as self-instruction, not a separately-invoked subagent.
**Fix**: none yet — this needs investigating in a future session (possibly the custom-agent discovery requires a session that starts with cwd inside the repo, or this harness surface just doesn't support project-level `.claude/agents/` the way the standard Claude Code CLI does). Flagging it plainly rather than continuing to imply real delegation was happening.
**Lesson**: two lessons here, both worth keeping. (1) On design taste: "avoid AI slop" was interpreted as "stay minimal and monochrome," but that's not what the brief actually needed — the earlier `docs/landing-page-brief.md` conflated restraint with authenticity. Real fix: color and motion are fine, even good, as long as they're specific to the product (per-line identity colors, a real animated rail map) rather than generic (an arbitrary gradient blob, decorative stock animation). Updated the brief accordingly. (2) On process: a single agent silently role-playing multiple "departments" doesn't get the benefit real delegation would (fresh eyes, genuine specialization) — it just re-runs the same judgment under different labels, which likely contributed to the first landing page playing it safe. Worth resolving the subagent-discovery issue before claiming the org structure is actually operating as designed.

---

## 2026-07-30 — Week 2: static GTFS, corrected line codes, frontend build
**Agent**: data-engineer-agent, backend-engineer-agent, frontend-engineer-agent
**Did**:
- Discovered NJ Transit's static rail GTFS feed (`https://www.njtransit.com/rail_data.zip`) is genuinely public — no auth required, unlike the GTFS-RT RailData API. Downloaded and parsed it for real.
- Used the real `routes.txt`/`stops.txt`/`stop_times.txt`/`trips.txt` to **replace guessed line names with verified data**: joined stop_times → trips → routes for Newark Penn Station (stop_id 107) and Newark Broad Street (stop_id 106) to get the actual route_short_name codes serving each station (NEC/NJCL/NJCLL/RARV at Penn; BNTN/BNTNM/MNE/MNEG at Broad St). Updated `NEWARK_AREA_LINES` in both `/ingestion/config.py` and `/backend/config.py` accordingly.
- Built and verified `/ingestion/static_gtfs_loader.py` (Route/Stop reference tables) against the real downloaded feed (17 routes, 231 stops parsed correctly).
- Updated `/backend`'s `/lines` endpoint to return `{code, display_name}` using a verified route_short_name → route_long_name mapping; re-verified all endpoints still pass.
- Scaffolded `/frontend` (Next.js, TypeScript, Tailwind via `create-next-app`). Built the landing page per `docs/landing-page-brief.md` and a dashboard shell polling the live backend. Design tokens use the `dataviz` skill's validated palette, with status colors mapped to on-time/delay semantics.
- Verified everything for real: `npm run lint` and `npm run build` both pass clean; ran the production build and the FastAPI backend together locally, loaded both pages in-browser (dark and light theme), and confirmed the dashboard's line picker and live-status table genuinely round-trip through the real API — not just visually inspected.
**Issue**: `create-next-app`'s new `react-hooks/set-state-in-effect` lint rule flagged the first draft of `ThemeToggle.tsx` (calling `setState` synchronously inside a `useEffect` to hydrate theme from `localStorage`).
**Root cause**: that pattern is exactly what the rule exists to catch — `localStorage` is external state, and synchronizing external state into React via an effect-that-calls-setState causes an extra render pass; React's own guidance is to use `useSyncExternalStore` for this case instead.
**Fix**: rewrote `ThemeToggle` around `useSyncExternalStore` (subscribing to a custom event + the native `storage` event) with a plain DOM-sync effect for the `data-theme` attribute — no setState-in-effect, lint clean.
**Lesson**: when a static/public data source dependency turns out to be genuinely public (like the static GTFS feed vs. the gated RailData API), verify it immediately by actually downloading and parsing it rather than assuming the same access barrier applies everywhere — it upgraded several "TODO, unconfirmed" placeholders from Week 1 to verified facts and unblocked real Week 2 work without needing the still-pending NJT account.

---

## 2026-07-30 — Week 1 kickoff: ingestion + backend scaffolds
**Agent**: data-engineer-agent, backend-engineer-agent
**Did**:
- `/ingestion`: DB models (`trip_updates`, `weather_hourly`, `service_alerts`), NWS weather client + poller (verified live against `api.weather.gov` — 156 hourly periods fetched and parsed correctly for Newark's grid point OKX/27,42), and a scaffolded NJ Transit RailData client + poller.
- `/backend`: FastAPI app with `/health`, `/lines`, `/lines/{line}/live`, smoke-tested end-to-end against a throwaway SQLite DB (all endpoints, including the 404 path, behave correctly).
**Issue**: NJ Transit's RailData API has no publicly-visible documentation — the reference client (`github.com/jtarrio/raildata`) confirms the auth *pattern* (username/password → bearer token, auto-refreshed) but not exact endpoint paths or response field names. The full API reference is only visible after registering at developer.njtransit.com.
**Root cause**: NJT gates its API docs behind developer account registration; no public mirror of the reference exists.
**Fix**: Wrote `njt_client.py`/`poll_gtfs_rt.py` as a structurally-correct scaffold (token caching/refresh, clean request methods) with every unconfirmed endpoint path and response field explicitly marked `TODO` rather than silently guessing and hoping. Shipped and verified the weather half of ingestion fully, since that API is public and needed no guesswork.
**Lesson**: When a required external API's docs are gated behind an account we don't have yet, don't fabricate exact request/response shapes — scaffold the correct *pattern* with loud TODOs instead. It keeps the code honest and makes the follow-up work (once Kartikey registers and can see the real docs) a quick fill-in rather than a debugging session against silently wrong assumptions.

**Manual follow-up needed from Kartikey** (see `/ingestion/README.md` for full steps):
1. Register at https://developer.njtransit.com/registration/ for RailData API access; add credentials to `/ingestion/.env`.
2. Once registered, resolve the `TODO`s in `njt_client.py`/`poll_gtfs_rt.py` against the real API reference.
3. Create a free Supabase project; add the Postgres connection string to `/ingestion/.env` and `/backend/.env`.

---

## 2026-07-30 — Repo scaffold (Day 0)
**Agent**: pm-agent
**Did**: Created the `ontrack-newark` repo structure (`/ingestion`, `/backend`, `/ml`, `/frontend`, `/infra`), wrote `CLAUDE.md`, `AGENTS.md`, `SKILLS.md`, this log, the 8 `.claude/agents/*.md` subagent definitions, and the 3 `.claude/skills/*` workflows (`deploy-check`, `ingest-verify`, `demo-prep`). No feature code yet.
**Lesson**: Setting up the org/tooling scaffold before any feature work means every subsequent agent invocation has a stable place to get context from (`CLAUDE.md` + `AGENTS.md`) instead of re-deriving project structure each time. Worth the up-front time given agents start cold on every call.
