# Same Supabase Postgres connection string used by /ingestion.
DATABASE_URL=postgresql://user:password@host:5432/postgres

# Comma-separated origins allowed to call this API (the deployed frontend URL, plus
# localhost for dev).
CORS_ORIGINS=http://localhost:3000
